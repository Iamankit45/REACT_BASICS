// import React from "react";

function add(x, y) {
  const z = x + y;
  return z;
}
function sub(x, y) {
    const z = x - y;
    return z;
  }
  function mul(x, y) {
    const z = x*y;
    return z;
  }
  function div(x, y) {
    const z = x/y;
    return z;
  }
  

export { add,sub,mul,div};
